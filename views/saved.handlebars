<title>{{title}}</title>

<header>
  <h1 class="welcome-title flex-container">Mongo DB Scraper!</h1>
  <h2 class="article">Articles:</h2>
</header>
<section class="btn-section">

  <a href="/" class="homeBtn">Home</a>

</section>
<main class="flex-main">
  <section class="left-side">
    <section class="flex-ul">
      <ul>
        {{#each saved}}
        <li>
          <div class="card" id="articles">
            <img src="{{image}}" />
            <div class="article-container">
              <h2>{{title}}</h2>
              <p>{{summary}}</p> <a href={{link}} class="siteLink">Check out the Article</a>
              <div class="notes-container">
                <a href="#notes">
                <div class="circle">
                  <div class="addNotes" data-id="{{_id}}"> Add a Note</div>
                </div>
                </a>
                <div class="circle">
                  <div class="delete" data-id="{{_id}}">Delete Article</div>
                </div>
              </div>
            </div>
          </div>
        </li>
        {{/each}}
      </ul>
    </section>
  </section>
    
  <section class="right-side">
    <div class="notes-div"></div>
  </section>
</main>